#include <iostream>
using namespace std;


int main() 
{
    //Bitwise operators it like and in the example , 
    //it takes the first number and convert it to binary and the second one convert it to binary then
    // it will do the bitwise between the bits if and we need to find to ones to 
    //but it in the final result otherwise we but zero then the result the compiler convert it to decimal
    bool result = 12 || 25;
    cout << "Result: " << result << endl ;
}